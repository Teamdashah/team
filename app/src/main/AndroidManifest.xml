<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myteam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myteam">
        <activity android:name=".click_res"
            android:parentActivityName=".restaurant_list"></activity>
        <activity android:name=".attractions_list" />
        <activity android:name=".restaurant_list" />
        <activity android:name=".hotel_list" android:parentActivityName=".input_schedule"/>
        <activity android:name=".input_schedule" />
        <activity
            android:name=".googleSignIn"
            android:exported="true" />
        <!--<activity android:name=".input_schedule" />-->
        <activity android:name=".forget_pwd" />
        <activity android:name=".register" />
        <activity android:name=".login" />
        <activity android:name=".hotel_main"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>